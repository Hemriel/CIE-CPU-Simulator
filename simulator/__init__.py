"""Package marker for the cpu module.

The __init__.py file tells Python that this directory is a package, allowing
imports like 'from cpu import cpu'. Even in Python 3.3+, explicit
__init__.py files are preferred for clarity and compatibility.
"""
